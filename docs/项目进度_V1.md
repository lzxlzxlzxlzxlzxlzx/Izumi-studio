# Izumi Studio 项目进度 V1

> 最后更新：2026-05-03
> 用途：实时反映各模块完成度、前后端对接状态、待办事项

---

## 1. 总览

| 层级 | 已完成 | 总计 | 进度 |
|------|--------|------|------|
| 后端模块 | 10 | 10 | **100%** |
| 前端页面 | 8.5 | 10 | ~85% |
| 前后端对接 | 5 | 7 | ~70% |

---

## 2. 后端模块

### 2.1 核心管道模块

| 模块 | 文件 | 状态 | 测试 | 备注 |
|------|------|------|------|------|
| LLMRouter | `services/llm_router.py` | ✅ 完成 | 手动验证 | DeepSeek/DashScope/Kimi，SSE流式+tool calling |
| ContextAssembler | `services/context_assembler.py` | ✅ 完成 | 手动验证 | system prompt + 历史 + worldbook 注入 + token裁剪 |
| PromptManager | `services/prompt_manager.py` | ✅ 完成 | 10/10 | 14条默认提示，排序/过滤/覆盖 |
| WorldBookEngine | `services/worldbook_engine.py` | ✅ 完成 | 17/17 | 关键词扫描 + 时序效果 + token预算 |
| MemorySystem | `services/memory_system.py` | ✅ 完成 | 10/10 | 短期记忆 + 增量摘要 |
| CharacterRegistryEngine | `services/character_registry.py` | ✅ 完成 | 13/13 | 事件溯源 + 5个skill + rollback |
| TemplateRenderer | `services/template_renderer.py` | ✅ 完成 | 15/15 | Handlebars 兼容模板（#if/#unless/#each + setvar/getvar/trim宏） |
| InstructFormatter | `services/instruct_formatter.py` | ✅ 完成 | 14/14 | 指令模板序列格式化 + 禁用fallback |

### 2.2 辅助模块

| 模块 | 文件 | 状态 | 测试 | 备注 |
|------|------|------|------|------|
| CardManager | `services/card_manager.py` | ✅ 完成 | 手动验证 | CRUD + 搜索 |
| STImporter | `services/st_importer.py` | ✅ 完成 | 手动验证 | ST格式转换（世界书/预设/角色卡） |
| CharacterTemplateManager | `services/character_template_manager.py` | ✅ 完成 | 9/9 | NPC模板分离 + 实例化 |

### 2.3 API 路由

| 路由 | 文件 | 状态 | 备注 |
|------|------|------|------|
| `/api/cards` | `routers/cards.py` | ✅ 完成 | CRUD + PATCH 更新 |
| `/api/sessions` | `routers/sessions.py` | ✅ 完成 | CRUD + 角色注册表 |
| `/api/chat` | `routers/chat.py` | ✅ 完成 | 消息CRUD + SSE流式 |
| `/api/import` | `routers/import_routes.py` | ✅ 完成 | ST文件导入 |
| `/api/presets` | `routers/presets.py` | ✅ 完成 | 列表/详情/删除 |
| `/api/worldbooks` | `routers/worldbooks.py` | ✅ 完成 | 列表/详情/删除 |
| `/api/upload` | `routers/upload.py` | ✅ 完成 | 图片上传 + Qwen-VL分析 |

### 2.4 数据层

| 组件 | 文件 | 状态 | 备注 |
|------|------|------|------|
| SQLite Schema (14表) | `db/schema.py` | ✅ 完成 | 自启动初始化 |
| Pydantic Models | `models/` | ✅ 完成 | 与TS类型对齐 |
| Config (.env加载) | `config.py` | ✅ 完成 | 无python-dotenv依赖 |

---

## 3. 前端

### 3.1 已实现

| 页面/组件 | 文件 | 状态 | 备注 |
|-----------|------|------|------|
| App 路由 | `App.tsx` | ✅ | `/cards`, `/chat/:sessionId`, 404 |
| 卡片画廊 | `pages/CardGalleryPage.tsx` | ✅ | 搜索、标签过滤、网格展示 |
| 聊天页 UI | `pages/ChatPage.tsx` | ✅ | SSE流式接入，支持token实时渲染 + tool_call展示 + 角色面板 |
| 卡片详情/编辑 | `pages/CardDetailPage.tsx` | ✅ | 查看/编辑角色卡、NPC展示、标签管理 |
| 角色注册表面板 | `components/CharacterRegistryPanel.tsx` | ✅ | 侧边抽屉展示活跃/非活跃角色及属性 |
| API 客户端 | `api/client.ts` | ✅ | 完整 REST + SSE streamChat + fetchCharacters + updateCard |
| 类型定义 | `types/index.ts` | ✅ | 与Pydantic对齐 |
| 状态管理 | `stores/sessionStore.ts` | ✅ | Zustand + streaming state |
| 脚手架 | `vite/react/tailwind/antd` | ✅ | 暗色主题 |
| ST 导入页 | `pages/ImportPage.tsx` | ✅ | 文件拖拽上传，支持角色卡/世界书/预设 |
| 设置页 | `pages/SettingsPage.tsx` | ✅ | 模型选择、温度、token、记忆、通知配置 |
| 预设管理 | `pages/PresetsPage.tsx` | ✅ | 预设列表/查看详情/删除 + 侧抽屉预览 |
| 世界书管理 | `pages/WorldbooksPage.tsx` | ✅ | 世界书列表/条目预览/删除 + 侧抽屉详情 |
| 图片上传 | `ChatPage` 内 | ✅ | 选图 → 上传 → Qwen-Plus分析 → 注入用户消息 |

### 3.2 待实现

| 功能 | 优先级 | 备注 |
|------|--------|------|
| ~~SSE 流式接入~~ | ~~P0~~ | ✅ 已完成 — `streamChat` 函数 + ChatPage SSE 实时渲染 |
| ~~消息发送流程修复~~ | ~~P0~~ | ✅ 已完成 — 用户输入 → SSE流式token → done保存 → 完整闭环 |
| ~~tool_call 渲染~~ | ~~P1~~ | ✅ 已完成 — `ToolCallBadge` 组件展示技能调用 |
| ~~角色面板~~ | ~~P1~~ | ✅ 已完成 — `CharacterRegistryPanel` 侧边抽屉展示角色及属性 |
| ~~卡片详情/编辑页~~ | ~~P1~~ | ✅ 已完成 — `CardDetailPage` 支持查看/编辑/保存 |
| ~~ST 导入页~~ | ~~P2~~ | ✅ 已完成 — `ImportPage` 拖拽上传 ST 格式文件 |
| ~~设置页~~ | ~~P2~~ | ✅ 已完成 — `SettingsPage` 模型/温度/记忆/通知 |
| ~~预设管理页~~ | ~~P2~~ | ✅ 已完成 — `PresetsPage` 列表/详情/删除 |
| ~~世界书管理页~~ | ~~P2~~ | ✅ 已完成 — `WorldbooksPage` 列表/条目预览/删除 |
| ~~图片上传~~ | ~~P2~~ | ✅ 已完成 — ChatPage 内 base64 上传 + Qwen-Plus 分析 |
| Swipe 切换 | P2 | 消息多版本滑动切换 |

---

## 4. 前后端对接状态

| 接口 | 后端 | 前端调用 | 对接 |
|------|------|----------|------|
| `GET /api/cards` | ✅ | `fetchCards()` | ✅ 已对接 |
| `GET /api/cards/:id` | ✅ | `fetchCard()` | ✅ |
| `GET /api/sessions/card/:cardId` | ✅ | `fetchSessions()` | ✅ |
| `POST /api/sessions` | ✅ | `createSession()` | ✅ |
| `DELETE /api/sessions/:id` | ✅ | `deleteSession()` | ✅ |
| `GET /api/chat/messages/:sessionId` | ✅ | `fetchMessages()` | ✅ |
| `POST /api/chat` | ✅ | `sendMessage()` | ⚠️ 只存消息不触发生成 |
| `POST /api/chat/stream/:sessionId` | ✅ | `streamChat()` | ✅ 已对接 |
| `POST /api/chat/rollback/:sessionId` | ✅ | `rollbackSession()` | ✅ |
| `GET /api/sessions/:id/characters` | ✅ | `fetchCharacters()` | ✅ 已对接 |
| `PATCH /api/cards/:id` | ✅ | `updateCard()` | ✅ 已对接 |
| `POST /api/import/*` | ✅ | `ImportPage` 上传 | ✅ 已对接 |
| `GET /api/presets` | ✅ | `fetchPresets()` | ✅ 已对接 |
| `GET /api/presets/:name` | ✅ | `getPreset()` | ✅ 已对接 |
| `DELETE /api/presets/:name` | ✅ | `deletePreset()` | ✅ 已对接 |
| `GET /api/worldbooks` | ✅ | `fetchWorldbooks()` | ✅ 已对接 |
| `GET /api/worldbooks/:id` | ✅ | `getWorldbook()` | ✅ 已对接 |
| `DELETE /api/worldbooks/:id` | ✅ | `deleteWorldbook()` | ✅ 已对接 |
| `POST /api/upload/image` | ✅ | `uploadImage()` | ✅ 已对接 |

---

## 5. 当前待办

### P0 — 阻塞性
- [x] **前端 SSE 流式接入**：`streamChat()` + ChatPage SSE token 实时渲染 ✅
- [x] **消息流程打通**：用户输入 → SSE 流式 token 渲染 → done 时保存 → 完整闭环 ✅

### P1 — 核心体验
- [x] tool_call 渲染 UI ✅
- [x] 角色注册表面板 UI ✅
- [x] 卡片详情/编辑页 ✅

### P2 — 增强功能
- [x] 预设/世界书管理页 ✅
- [x] ST 导入页 ✅
- [ ] Swipe 切换
- [x] 图片上传 ✅
- [x] 设置页 ✅

---

## 6. 测试覆盖

| 模块 | 单元测试 | 集成测试 |
|------|----------|----------|
| PromptManager | 10/10 ✅ | — |
| WorldBookEngine | 17/17 ✅ | — |
| MemorySystem | 10/10 ✅ | — |
| CharacterRegistryEngine | 13/13 ✅ | — |
| TemplateRenderer | 15/15 ✅ | — |
| InstructFormatter | 14/14 ✅ | — |
| CharacterTemplateManager | 9/9 ✅ | — |
| LLMRouter | 手动验证 ✅ | 2轮对话测试通过 |
| ContextAssembler | 手动验证 ✅ | — |
| **合计** | **88 通过** | — |

---

## 7. 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-02 | 创建进度文档；完成 PromptManager、WorldBookEngine、MemorySystem、CharacterRegistryEngine 四个模块及测试 |
| 2026-05-02 | 完成 TemplateRenderer（15测试）、InstructFormatter（14测试）、CharacterTemplateManager（9测试）；后端模块 10/10 全部完成，合计 88 测试通过 |
| 2026-05-03 | **P0 突破**：SSE 流式接入完成 — `client.ts` 新增 `streamChat()` 函数，`ChatPage.handleSend` 重写为 fetch+ReadableStream SSE，`sessionStore` 新增 streaming 状态，`ToolCallBadge` 组件渲染 tool_call。前端构建通过 (626KB) |
| 2026-05-03 | **P1 完成**：角色注册表面板 `CharacterRegistryPanel`（侧边抽屉 + 活跃/非活跃分组）、卡片详情/编辑页 `CardDetailPage`（查看/编辑/NPC展示/保存）；后端新增 `GET /api/sessions/{sid}/characters`、`PATCH /api/cards/{id}`、`card_manager.update_card()`。前端构建通过 (751KB) |
| 2026-05-03 | **P2 推进**：ST 导入页 `ImportPage`（拖拽上传角色卡/世界书/预设 JSON）、设置页 `SettingsPage`（模型/温度/记忆/通知配置 localStorage）；CardGallery 顶部栏新增 Import/Settings 导航入口。前端构建通过 (919KB) |
| 2026-05-03 | **P2 收尾**：预设管理 API (`/api/presets` list/get/delete) + `PresetsPage`（列表/详情抽屉/删除）；世界书管理 API (`/api/worldbooks` list/get/del) + `WorldbooksPage`（列表/条目预览/删除）；图片上传 API (`/api/upload/image` — 保存 + base64 → Qwen-VL 分析 → 返回描述) + ChatPage 选图上传触发分析；CardGallery 顶部栏新增 Presets/Worldbooks 入口。前端构建通过 (941KB)。仅剩 Swipe 一项 |
